#include<stdio.h>
int main(){
	int a,b;
	printf("cho mot so a:\n");
	scanf("%d",&a);
	printf("cho mot so b:\n");
	scanf("%d",&b);
	if(a>b){
		printf("in ra man so lon :%d",a);
	}else if(a<b) {
	printf("in ra man so lon:%d",b);
	
	}
	return 0;
}
